using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Tasowansko_kart : MonoBehaviour
{
    

        // public List<Karta> Tasowanie(List<Karta> kartas)
    // {
        // for(int i=0; i<kartas.Count; i++)
        // {
            // Karta K = kartas[i];
            // kartas.Remove(kartas[i]);
            // int losowe = UnityEngine.Random.Range(0, kartas.Count);
                // kartas.Insert(losowe, K);
        // }
        // return kartas;
    // }

    // void Start()
    // {   List<Karta> kartas=new List<Karta>();
        // kartas.Add(new Karta("pierwsza"));
        // kartas.Add(new Karta("druga"));
        // kartas.Add(new Karta("trzecia"));
        // kartas.Add(new Karta("czwarta"));
        // kartas = Tasowanie(kartas);
        // Pokaz(kartas);
    // }
    // public void Pokaz(List<Karta> kartas)
    // {
        // foreach (Karta K in kartas)
        // {
            // Debug.Log(K.nazwa);
        // }
    // }
}
